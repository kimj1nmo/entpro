<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.page.shop.mapper.AddressMapper">

    <!-- ✅ 특정 회원의 주소 조회 -->
    <select id="selectAddressesByMemId" parameterType="String" resultType="kr.or.ddit.vo.AddressVO">
        SELECT ADD_NO AS addNo,
               MEM_ID AS memId,
               ADD_NAME AS addName,
               ADD_ZIP AS addZip,
               ADD_1 AS add1,
               ADD_2 AS add2,
               ADD_DATE AS addDate,
               SN AS sn,
               ADD_TEL AS addTel
        FROM ADDRESS
        WHERE MEM_ID = #{memId}
        ORDER BY ADD_DATE DESC
    </select>

</mapper>
