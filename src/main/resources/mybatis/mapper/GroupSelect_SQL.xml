<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.ddit.page.companypage.manageshop.GroupsMapper">

<select id="selectGroups" resultType="kr.or.ddit.vo.GroupsVO">
    SELECT 
        GROUP_NO AS groupNo, 
        GROUP_NAME AS groupName
    FROM GROUPS
    ORDER BY GROUP_NO
</select>

</mapper>
